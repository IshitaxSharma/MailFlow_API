# MailFlow_API
A lightweight FastAPI service for validating and processing email requests. MailFlow API provides a simple backend structure for receiving email data, including the sender, recipients, subject, and message body. It uses FastAPI and Pydantic to validate incoming requests and demonstrates the fundamentals of building a RESTful API.
